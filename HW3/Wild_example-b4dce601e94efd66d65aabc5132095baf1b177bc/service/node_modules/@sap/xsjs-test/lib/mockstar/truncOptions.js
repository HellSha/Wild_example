/**
 * Set of TruncOption constants.
 * @enum TruncOptions
 * @memberof module:mockstar
 */
var TruncOptions = {
    /**
     * The target package gets prefixed with the package of the original model
     *
     * @type {String}
     * @constant
     */
    FULL : 'full',
    /**
     * the target package is used
     *
     * @type {String}
     * @constant
     */
    NONE : 'none'
}

module.exports = TruncOptions