#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@sap/xsjs-test/bin/xstest" "$@"
  ret=$?
else 
  node  "$basedir/../@sap/xsjs-test/bin/xstest" "$@"
  ret=$?
fi
exit $ret
